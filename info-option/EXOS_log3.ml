(* Exercice 6.9 *)
(* a *)
(* celui qui pose la question est un positif l'autre un negatif *)

(* b *)
(* la question est valide l'autre non *)

(* c *)
(* personne ne peut poser la question Zorn et donc negatif *)

(* d *)
(* A N   B P *)

(* e *)
(* B N => A P  C N       B P => A N C N *)

(* f *)
(* P N     N N *)

(* Exercice 6.10 *)
(* A => A     \B <=> \C   2 => A    donc A  *)

(* Plomb car sinon 2 tableau *)







(* \A  \A et \B   B*)
(* (P1 and P3) xor P2 *)
(* (\A and B) xor (\A and \B) *)
(* ((\A and B) or (\A and \B)) and \((\A and B) and (\A and \B)) *)
(* ((\A and B) or (\A and \B)) and ((A or \B) or (A or B)) *)
(* (\A and B) or (\A and \B) *)
(* \A and (B or \B) *)
(* \A *)



(* (I1 and I2 and I3) or (\I1 and \I2 and \I3) *)
(* I1 = C1 and C3   I2 = C2 => \C3  I3 = C1 and \C2*)




(*  (\A1 and A2 and A3) or (A1 and \A2 and A3) or (A1 and A2 and \A3) *)

(* A1 = B => N    A2 = B and N   A3 = N *)

(* ((B and \N) and (B and N) and N) or ((\B or N) and (\B or \N) and N) or ((\B or N) and (B and N) and \N)*)
(* \B and N  *)



(* A1 = P and \G  A2 = \M and G  A3 = \G and (\P or \M)*)

(* P and \G and \M and (G or (P and M)) *)
(* P and \G and \M)) *)

